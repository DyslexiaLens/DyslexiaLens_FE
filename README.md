# Capstone Frontend (Scaffold)

This is a scaffolded Vite + React (JavaScript) project with Tailwind CSS prepared for mapping your Figma designs.

Quick start:

```bash
npm install
npm run dev
```

Next steps:
- Confirm the MCP/Figma assets are accessible so I can import tokens, fonts, and SVGs into `src/assets/figma`.
- I will map each Figma frame to `src/pages/` and `src/components/` and implement design tokens into Tailwind.
